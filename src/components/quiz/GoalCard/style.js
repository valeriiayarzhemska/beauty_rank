import { StyleSheet } from 'react-native';

export const styles = () => {
  return StyleSheet.create({
    titlesContainer: {
      marginBottom: 8,
      paddingTop: 30,
      width: '100%',
    },
  });
};
