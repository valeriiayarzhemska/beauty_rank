import { StyleSheet } from 'react-native';

export const styles = () => {
  return StyleSheet.create({
    subtitleContainer: {
      marginTop: 16,
      width: '100%',
      justifyContent: 'center',
    },
    inputContainer: {
      position: 'relative',
      marginTop: 16,
    },
    fullWidth: {
      width: '100%',
    },
    halfWidth: {
      width: '48.2%',
    },
  });
};
