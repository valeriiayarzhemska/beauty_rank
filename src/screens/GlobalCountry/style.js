import { StyleSheet } from 'react-native';

export const styles = () => {
  return StyleSheet.create({
    container: {
      height: '100%',
      flex: 1,
      width: '100%',
    },
    contentContainer: {
      flexGrow: 1,
      paddingBottom: 220,
      gap: 82,
    },
  });
};
