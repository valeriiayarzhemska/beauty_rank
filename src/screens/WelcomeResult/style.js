import { StyleSheet } from 'react-native';

export const styles = () => {
  return StyleSheet.create({
    scrollContainer: {
      flexGrow: 1,
    },
    container: {
      flexGrow: 1,
      gap: 10,
      paddingBottom: 50,
    },
  });
};
