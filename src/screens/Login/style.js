import { StyleSheet } from 'react-native';
import { colors } from '../../constants/constants';

export const styles = () => {
  return StyleSheet.create({
    scrollView: {
      alignItems: 'center',
      paddingTop: 30,
      width: '100%',
    },
    signUpContainer: {
      alignItems: 'center',
      width: '100%',
      marginTop: 44,
      marginBottom: 24,
    },
  });
};
