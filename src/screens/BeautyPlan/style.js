import { StyleSheet } from 'react-native';

export const styles = () => {
  return StyleSheet.create({
    finishContainer: {
      position: 'absolute',
      top: 0,
      right: 0,
      width: '100%',
      height: '100%',
      zIndex: 10,
    },
  });
};
